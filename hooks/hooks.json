{
  "description": "clouter: route visual prompts (image, SVG, video, speech) to Jev-ranked OpenRouter models",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for p in python3 python py; do \"$p\" -c '' </dev/null 2>/dev/null && exec \"$p\" \"${CLAUDE_PLUGIN_ROOT}/skills/visual/route.py\"; done; exit 0",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
